{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Create-react-app/package.json",
                "localRoot": "${workspaceFolder}/Create-react-app"
            }
        }
    ]
}